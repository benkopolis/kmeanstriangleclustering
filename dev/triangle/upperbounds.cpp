#include "upperbounds.h"

UpperBounds::UpperBounds(std::unordered_map<unsigned, unsigned> *pointsToClusters):
    pointsToClusters(pointsToClusters)
{

}

void UpperBounds::update(const QVector<Distance>& delta)
{

}
